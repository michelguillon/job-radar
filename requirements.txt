anthropic>=0.40.0
requests>=2.32.0
beautifulsoup4>=4.12.0
PyYAML>=6.0.0
pytest>=8.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# Observability (opt-in) — Langfuse v4, OTel-based (SPEC_LANGFUSE_INSTRUMENTATION §3).
# Tracing is a clean no-op when LANGFUSE_PUBLIC_KEY is unset, so tests + the pipeline run
# untraced (cli/telemetry.py). The SDK is imported lazily, inside functions only.
langfuse>=4.0.0
