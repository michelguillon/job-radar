services:
  job-radar:
    build: .
    image: job-radar
    env_file:
      - path: .env
        required: false
    volumes:
      - .:/app
    working_dir: /app
    # Override with: docker compose run --rm job-radar python -m cli.collect ...
    command: ["python", "--version"]
